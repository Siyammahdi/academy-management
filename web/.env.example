# Browser-facing API base. Same-origin path avoids CORS and LAN `localhost` bugs.
NEXT_PUBLIC_API_URL=/api/v1

# Nest origin for Next.js rewrites (no /api/v1 suffix).
# Local:  http://127.0.0.1:4000
# Prod:   https://academy-management.onrender.com
API_PROXY_TARGET=http://127.0.0.1:4000
